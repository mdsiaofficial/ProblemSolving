#include <iostream>
#include <vector>
#include <map>
#include <cmath>
using namespace std;
// unsolved
int main() {
    double a,b;
    cin>>a>>b;

    double f = pow(a,b)-pow(b,a);
    cout<<f<<endl;
    return 0;
}