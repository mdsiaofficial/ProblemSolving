#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;

int main()
{
/* 
    int t;
    int n;
    cin >> t;
    
    
    for (int i = 1; i <= t; i++){
        float total = 0.0;
        cin >> n;
        
        for (int j = 1; i <= n; j++){
            float x;
            cin >> x;
            total = total + x;
        }
        cout << fixed << setprecision(3) <<"Case "<<i<<": "<< (total / (float)n) << endl;
    }
    
 */

/* 
//COPIED

    int t, n;
    double p;
    cin >> t;
    for(int i = 1; i <= t; i++)
    {
        double sum = 0.0;
        cin >> n;
        for(int i = 0; i < n; i++)
        {
            cin >> p;
            sum += p;

        }
        printf("Case %d: %.3lf\n", i, (sum/(double)n));

    }


  */

    // unsigned long long power = pow(10, 10);
    // cout << power;

    return 0;
}
