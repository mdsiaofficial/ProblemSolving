#include <bits/stdc++.h>
using namespace std;
int main()
{

    // perimeter of a rectangle, P = 2 ( a+b)

    int a, b;
    cin >> a >> b;
    cout << a * b << " " << 2 * (a + b) << endl;
    return 0;
}