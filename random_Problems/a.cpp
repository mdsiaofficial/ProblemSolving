#include <bits/stdc++.h>
#include <iostream>
#include <iomanip>
#include <stdlib.h>
using namespace std;
int main()
{
    int a, b;
    cin >> a >> b;

    float s = (float)b / (float)a;

    cout << fixed << setprecision(3) << s << endl;

    return 0;
}
